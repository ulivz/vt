sidebar-active-bg() {
  color: var(--vp-c-brand);
  background-color: rgba(100, 100, 100, 0.1);
  transition: background-color 0.1s;
  border-radius: 8px;
  margin-left: -10px;
  padding-left: 10px;
}